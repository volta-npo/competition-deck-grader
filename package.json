{
  "name": "@volta-oss/competition-deck-grader",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "An open rubric runner for Volta’s Digital Transformation Challenge pitch decks.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "test:rust": "cargo test --manifest-path backend/rust/Cargo.toml",
    "test:backend": "npm run test:rust",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/competition-deck-grader:local .",
    "docker:run": "docker compose up --build",
    "serve:rust": "cargo run --manifest-path backend/rust/Cargo.toml -- serve"
  },
  "keywords": [
    "digital-equity",
    "local-first",
    "oss",
    "student agency operations",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/competition-deck-grader.git"
  },
  "homepage": "https://volta-npo.github.io/competition-deck-grader/",
  "bugs": {
    "url": "https://github.com/volta-npo/competition-deck-grader/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.0.0"
  }
}
